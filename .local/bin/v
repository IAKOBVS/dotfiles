#!/usr/bin/dash
file=$(realpath $(echo $@ | awk '{print $NF}'))
if [ -f $file ]; then
	:
else
	if touch $file; then
		echo $file >> /tmp/__fzfhome__ &
		echo $file >> /tmp/__fzfroot__ &
	fi
fi
nvim $@
