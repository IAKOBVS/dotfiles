#!/usr/bin/dash
pathToFile=$(find "$HOME"/.local/bin "$HOME"/c ! -path '*vscode*' ! -path '*git*' ! -name '?' -type f -print 2> /dev/null | fzfp --delimiter / --with-nth -1)
test "$pathToFile" && nvim "$pathToFile" && cd "$(dirname "$pathToFile")" || return
