#!/usr/bin/dash
pathToFile=$(find $HOME/.local/bin $HOME/c ! -path '*cnlp*data*' -a ! -path '*vscode*' -a ! -path '*git*' -a ! -name '.' -type f -print 2> /dev/null | fzfp --delimiter / --with-nth -1)
test "$pathToFile" && nvim "$pathToFile" && cd "$(dirname "$pathToFile")" || return
