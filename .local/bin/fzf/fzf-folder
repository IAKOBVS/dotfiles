#!/usr/bin/dash
pathToFile=$(find "$@" \( -name .git \) -prune -o -type f -print | fzfp --delimiter / --with-nth -1)
nvim "$pathToFile" && cd "$(echo "$pathToFile" | rev | cut -d'/' -f2- | rev)" || return
