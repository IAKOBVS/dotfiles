#!/usr/bin/dash
if test "$1"; then
	case $1 in
		$HOME/*)
			test -f "$1" && set -- "$(dirname "$1")";;
		*)
			if test -f $1; then
				set -- "$(dirname "$1")"
			else
				[ -d $1 ] || set -- "$HOME/$1"
			fi
	esac
fi
builtin cd $1 || return
