#!/usr/bin/dash
if test "$1"; then
	case $1 in
		$HOME/*)
			test -f "$1" && 1="$(dirname "$1")";;
		*)
			if test -f $1; then
				1="$(dirname "$1")"
			else
				[ -d $1 ] || 1="$HOME/$1"
			fi
	esac
fi
builtin cd $1 2> /dev/null || return
