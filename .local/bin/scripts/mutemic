#!/bin/sh
pamixer --source $(getmicsource) -t
if [ $(ifmute) = 'true' ]; then
	notif='🔇 Mic is muted!'
else
	notif='🎤 Mic is on!'
fi
dunstctl close
notify-send "$notif"
gst-play-1.0 /usr/share/sounds/freedesktop/stereo/bell.oga
