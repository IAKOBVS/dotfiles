#!/bin/sh

set -u

CMD=$COMPOSITOR

# Do not execute if $COMPOSITOR already running
ps -e -o comm --no-headers | grep -qF "$CMD" && exit

gaming-kill
$COMPOSITOR &
dunst &
unclutter &
dwmblocks-fast &
nv-tearfree-on
