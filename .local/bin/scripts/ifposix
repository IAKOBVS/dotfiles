#!/bin/sh
if test ! $2
then
	echo 'Usage: ifposix <cmd> <flag>'
	return 1
fi
man $1 | grep "^[[:space:]]*$2[^A-Za-z]" || echo "System $1 doesn't support $2"
manp $1 | grep "^[[:space:]]*$2[^A-Za-z]" || echo "POSIX $1 doesn't support $2"
