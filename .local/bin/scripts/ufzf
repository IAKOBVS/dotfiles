#!/bin/sh
__find__(){
	find $1 \
	! -path '*cache*' \
	! -path '*.git*' \
	! -path '*.gnupg*' \
	! -path '*.vscode*' \
	! -path '*.rev*' \
	! -path '*.key*' \
	> $2 2> /dev/null
}
if ! ps -A | grep 'find' -q
then
	__find__ / /tmp/__fzfroot__ &
	__find__ $HOME /tmp/__fzfhome__ &
fi
