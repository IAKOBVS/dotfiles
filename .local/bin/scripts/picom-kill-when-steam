#!/bin/sh

set -u

[ -n $TERMINAL ] || exit 1

# Exit if terminal already running
ps -e -o comm | grep -qF -e "$TERMINAL" && exit
# Exit if steam is not running
ps -e -o comm | grep -qF -e 'steam' || exit

gaming-kill
