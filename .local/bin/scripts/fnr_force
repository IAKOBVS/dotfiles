#!/bin/sh
if test ! $3
then
 	echo 'Usage: sed <dir> <find> <replace>'
	return
fi
if test $(uname -s) = 'Linux'
then
	gw $2 $(find "$1" -type f)
	find "$1" -type f -exec sed -i "s|$2|$3|g" {} \;
	exec echo "Replaced $2 with $3"
else
	exec echo 'sed -i is not available!'
fi
