#!/bin/sh
set -- "$(find $HOME/.local/bin/scripts $HOME/c -type f \
	! -path '.git*' \
	! -path '*.o' \
	! -name '?' \
	! -name '.vscode*' \
	! -name '.clang-format' \
	! -name '.gitignore' \
	2>/dev/null | fzfpreview --delimiter / --with-nth -1)"
[ $1 ] && v $1
