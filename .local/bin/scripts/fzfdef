#!/bin/sh
case $1 in
'') fzfdir | fzfpreview ;;
-e)
	shift
	fzfdir | fzfpreview $@
	;;
$HOME)
	shift
	grep $HOME/ /tmp/__fzfhome__ 2>/dev/null | fzfpreview $@
	;;
*)
	shift
	fzfdir | grep $(basename "$@")/ | fzfpreview $@
	;;
esac
