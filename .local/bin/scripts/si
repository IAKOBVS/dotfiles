#!/bin/sh
toGb=$((1024*1024))
du -s -- * \
| sort -n \
| awk -v fmt=$toGb \
'BEGIN { total = 0 } \
	{ total += $1; \
	printf "%.1fG %s\n", $1/fmt, $2 } \
END { printf("%.1fG total", total/fmt) }' \
| less
