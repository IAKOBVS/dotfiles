#!/bin/sh
nvidia-settings --query CurrentMetaMode | grep -qF 'ForceFullCompositionPipeline=On' && exit
nvidia-settings --assign CurrentMetaMode="nvidia-auto-select +0+0 { ForceFullCompositionPipeline=On }"
