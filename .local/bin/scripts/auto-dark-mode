#!/bin/sh
gethour()
{
	LC_TIME="C.UTF-8" date | awk '{ gsub(/:[:0-9]*/, ""); print $4 }'
}
while :; do
	if [ "$(gethour)" -lt 16 ]; then
		light-mode
		sleep $(((16 - $(gethour)) * 3600))
	else
		dark-mode
		exit
	fi
done
