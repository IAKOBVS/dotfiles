#!/bin/sh
gethour() {
	LC_TIME="C.UTF-8" date | awk '{ gsub(/:.*/, ""); print $4 }'
}
curr_hour=$(gethour)
if [ $curr_hour -lt 16];
then
	light-mode
	sleep $(((16 - $(gethour)) * 3600))
	auto-dark-mode
else
	dark-mode
fi
